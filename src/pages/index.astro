---
import BaseLayout from '../layouts/BaseLayout.astro';

const title = 'Home';
const description = 'Minimal Astro scaffold for GitHub Pages.';
---

<BaseLayout title={title} description={description}>
	<section class="hero">
		<p class="eyebrow">Small, deliberate, static</p>
		<h1>Hulk Playground</h1>
		<p class="lede">
			Built to stay lean and obvious. No filler, just the parts that ship.
		</p>
		<div class="actions">
			<a class="button" href={`${import.meta.env.BASE_URL}projects/`}>View projects</a>
			<a class="button ghost" href={`${import.meta.env.BASE_URL}about/`}>About this site</a>
		</div>
	</section>
	<section class="grid">
		<article>
			<h2>Purpose</h2>
			<p>Keep it light, keep it legible, and keep it moving.</p>
		</article>
		<article>
			<h2>Focus</h2>
			<p>Say the important thing first. Everything else can wait.</p>
		</article>
		<article>
			<h2>Stack</h2>
			<p>Astro, static output, no drama on deploy.</p>
		</article>
	</section>
</BaseLayout>
